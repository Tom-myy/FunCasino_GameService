spring:
  application:
    name: game-service
  profiles:
    active: dev  #default profile

#  cache:
#    type=none:#  profiles:

game:
  timer:
    betting-time: 15
    decision-time: 25

management:
  endpoints:
    web:
      exposure:
        include: health, info

jwt:
  game-secret: ${JWT_GAME_SECRET}
  system-secret: ${JWT_SYSTEM_SECRET}