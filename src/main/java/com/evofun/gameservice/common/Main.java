package com.evofun.gameservice.common;/*
package com.example.evoFunServer.common;


import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

public class Main {
    public static void main(String[] args) {
        BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();

        // Пример: допустим, это старые пароли в базе
        String[] plainPasswords = {"admin", "admin2", "admin3", "admin4", "admin5", "admin6"};

        for (String plain : plainPasswords) {
            String encoded = encoder.encode(plain);
            System.out.println("UPDATE users SET password = '" + encoded + "' WHERE password = '" + plain + "';");
        }
    }
}
*/
